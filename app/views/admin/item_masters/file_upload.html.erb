<%= form_tag('/admin/item_masters/import_csv', multipart: true) do %>
  <label for="file">File to Upload</label> <%= file_field_tag "file", :multiple => true %>

  <%= select_tag "uploding_type", options_for_select([ "All upload", "Price Upload" ])%>
  <div><%= submit_tag 'Save' %></div>
<% end %>
